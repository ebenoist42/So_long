int check_norm(char **map)
{
	check_wall(map);
}

static int check_wall(char **map)
{
	int	i;
	int h;

	h = ft_strlen(map -1);
	i = 0;
	while(i < h)
	{
		if(map[i] != '1');
			invalid_map(map);
	}
	while(map[i] - 1)
	{

	}
}